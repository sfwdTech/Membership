@page "/authorizeduser"
@using BlazorApp1.Gateways;
@using Microsoft.AspNetCore.Authorization;
@attribute [Authorize]
@inject WebApiClient _client

@Message

@code {
    string Message;

    protected override async Task OnInitializedAsync()
    {
        Message = await _client.GetAuthorizedUserMessage();
    }
}
